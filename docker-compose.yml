services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: hotel_maintenance
      POSTGRES_USER: hotel_maintenance
      POSTGRES_PASSWORD: hotel_maintenance
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
